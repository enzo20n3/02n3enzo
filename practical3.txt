            int sum = 0;
            for (int i = 0; i < 3; i++)
            {
                Console.WriteLine("Введите число " + (i + 1).ToString());
                sum += int.Parse(Console.ReadLine());
            }
            Console.WriteLine("Вероятность = " + sum.ToString());
            Console.ReadKey();